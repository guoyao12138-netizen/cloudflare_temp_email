name: Build InkBlue Writer APK

on:
  push:
    branches:
      - claude/android-fiction-writing-app-wgpg3s
    paths:
      - "android-writer/**"
      - ".github/workflows/android_writer_apk.yml"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: android-writer
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Build debug APK
        run: ./gradlew :app:assembleDebug --no-daemon

      - name: Publish APK to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=$(sed -n 's/.*versionName = "\(.*\)"/\1/p' app/build.gradle.kts)
          TAG="inkblue-writer-v${VERSION}"
          APK="inkblue-writer-v${VERSION}-debug.apk"
          cp app/build/outputs/apk/debug/app-debug.apk "$APK"
          if ! gh release view "$TAG" --repo "$GITHUB_REPOSITORY" >/dev/null 2>&1; then
            gh release create "$TAG" \
              --repo "$GITHUB_REPOSITORY" \
              --target "$GITHUB_SHA" \
              --title "墨蓝写作 InkBlue Writer v${VERSION}" \
              --notes "安卓网文创作 App。模仿纯纯写作的沉浸式写作体验，Claude 风格蓝色 UI。下载 APK 安装即可使用（调试签名，安装时需允许未知来源）。"
          fi
          gh release upload "$TAG" "$APK" --repo "$GITHUB_REPOSITORY" --clobber

  desktop-mac-intel:
    needs: build
    runs-on: macos-13
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Build DMG (Intel x64)
        run: gradle packageDmg --no-daemon -p android-writer/desktop

      - name: Upload DMG to release
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          VERSION=$(sed -n 's/.*versionName = "\(.*\)"/\1/p' android-writer/app/build.gradle.kts)
          TAG="inkblue-writer-v${VERSION}"
          DMG=$(ls android-writer/desktop/build/compose/binaries/main/dmg/*.dmg | head -n 1)
          cp "$DMG" "inkblue-writer-v${VERSION}-mac-intel.dmg"
          gh release upload "$TAG" "inkblue-writer-v${VERSION}-mac-intel.dmg" --repo "$GITHUB_REPOSITORY" --clobber
