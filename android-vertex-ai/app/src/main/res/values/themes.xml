<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme; the actual UI colors come from Compose (Theme.kt).
         We use a Material3 DayNight base and let the system bars follow. -->
    <style name="Theme.VertexChat" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
